import React from "react";

const SectionHeaderSecondary = ({ text }) => {
  return (
    <header className="section__header--secondary">
      <h1>{text}</h1>
    </header>
  );
};

export default SectionHeaderSecondary;
